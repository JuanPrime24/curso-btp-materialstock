_schema-version: "3.2"
ID: materialstock
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: materialstock-destination-content
  type: com.sap.application.content
  requires:
  - name: materialstock-destination-service
    parameters:
      content-target: true
  - name: materialstock-repo-host
    parameters:
      service-key:
        name: materialstock-repo-host-key
  - name: materialstock-uaa
    parameters:
      service-key:
        name: materialstock-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: materialstock_html_repo_host
          ServiceInstanceName: materialstock-html5-srv
          ServiceKeyName: materialstock-repo-host-key
          sap.cloud.service: materialstock
        - Authentication: OAuth2UserTokenExchange
          Name: materialstock_uaa
          ServiceInstanceName: materialstock-xsuaa-srv
          ServiceKeyName: materialstock-uaa-key
          sap.cloud.service: materialstock
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: materialstock-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: materialstock-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - materialstock.zip
      name: materialstock
      target-path: resources/
- name: materialstock
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: materialstock-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: materialstock-destination-service
    service-plan: lite
- name: materialstock-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: materialstock-xsuaa-srv
    service-plan: application
- name: materialstock-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: materialstock-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
